// test config with defaults
message-signer {
  kafka {
    url = "localhost:6001"
    topic {
      incoming = ["incoming"]
      outgoing {
        default = "outgoing"
      }
    }
    stopTimeout = 1 ms
  }

  certificate {
    path = "./cert.jks"
    password = "very-insercure-password"
    entryAlias = "signer-certificate"
  }
}