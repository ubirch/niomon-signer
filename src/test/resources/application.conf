// test config with defaults
kafka {
  url = "localhost:9992"
  topic {
    incoming: "incoming"
    outgoing: "outgoing"
  }
}

certificate {
  path = "./cert.jks"
  password = "very-insercure-password"
  entryAlias = "signer-certificate"
}
